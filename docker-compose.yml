services:
  load-monitor:
    image: load-monitor:latest
    build:
      context: .
      dockerfile: Dockerfile
    container_name: load-monitor
    ports:
      - "5000:5000"
    environment:
      - FLASK_ENV=production
    restart: unless-stopped
